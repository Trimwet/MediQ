# Bug Bounty Report — Final Hunt (Hero / Pricing / Auth / Supabase)

**Date:** 2026-08-31
**Scope — last untouched:** `src/features/landing/index.tsx` · `src/features/landing/components/pricing-section.tsx` · `src/routes/(auth)/sign-in.tsx` · `src/routes/(auth)/sign-up.tsx` · `src/features/auth/*` (sign-in / sign-up / forgot-password / auth-layout) · `supabase/config.toml` (`site_url`, `redirect_urls`, `JWT expiry`) · `src/lib/supabase.ts` (client creation, storage, realtime)
**Method:** Full manual read of every file in scope + `src/features/create-clinic/index.tsx` (pricing CTA target) + `src/data/supabase/repos.ts` (authRepository) + `src/hooks/use-supabase-auth-sync.ts` + `src/routes/_authenticated/route.tsx` (redirect chain) + `src/routes/patient.tsx` + `vite.config.ts` + env files. Checked: placeholder data vs real fetch, single-image carousel invariants, CTA `search: {plan}` wiring, Supabase error string matching for `email not confirmed` / `invalid credentials` / `429`, duplicate email handling, `?redirect=` open-redirect + preservation, hardcoded `127.0.0.1` / `getmediq.vercel.app` URL surfacing, Switch a11y, OTP removal. No fixes — report only.
**Files inspected:** `src/features/landing/index.tsx:1`, `src/features/landing/components/pricing-section.tsx:1`, `src/routes/(auth)/sign-in.tsx:1`, `src/routes/(auth)/sign-up.tsx:1`, `src/features/auth/auth-layout.tsx:1`, `src/features/auth/sign-in/components/user-auth-form.tsx:1`, `src/features/auth/sign-up/components/sign-up-form.tsx:1`, `src/features/auth/sign-up/components/sign-up-form.test.tsx:1`, `src/features/auth/sign-in/components/user-auth-form.test.tsx:1`, `src/features/auth/forgot-password/components/forgot-password-form.tsx:1`, `src/features/auth/forgot-password/index.tsx:1`, `src/features/change-password/index.tsx:1`, `src/features/create-clinic/index.tsx:1`, `src/lib/supabase.ts:1`, `src/data/supabase/repos.ts:615-661`, `src/hooks/use-supabase-auth-sync.ts:1`, `src/routes/_authenticated/route.tsx:1`, `src/routes/patient.tsx:1`, `supabase/config.toml:1` (at `C:/Users/MAFUYAI/Documents/MediQ/supabase/config.toml`), `supabase/functions/invite-staff/index.ts:1`, `vite.config.ts:1`, `.env.example:1`

---

## Executive Summary — Severity Counts

| Severity | Count | Examples |
|----------|-------|----------|
| **Critical** | 1 | Open redirect via unvalidated `?redirect=` → `navigate({to: redirectTo})` (AUTH-06) |
| **Important** | 7 | Hero stats all-zero credibility damage (HERO-01), sign-in missing `429 / too many requests` branch (AUTH-02), `additional_redirect_urls` uses `https://127.0.0.1:3000` (SUPA-02), `enable_confirmations=false` makes `email not confirmed` dead code (SUPA-04), `sign-up` top-level `window.location.search` SSR-brittle (AUTH-05), pricing Switch a11y label association (PRICE-02) |
| **Minor** | 8 | Single-image carousel timer churn (HERO-02), dot nav shown for 1 image, backgroundImage quoting (HERO-03), sign-up duplicate raw error (AUTH-04), forgot-password `window.location.origin` is correctly dynamic but enumerate-safe (AUTH-07), supabase.ts missing `flowType: pkce` explicit (SUPA-07) |
| **Info** | 5 | Pricing CTAs correctly wired to `/create-clinic?plan=` (PRICE-01), free banner intentional (PRICE-03), client uses `VITE_SUPABASE_*` env not hardcoded URL (URL-02), JWT 3600 standard (SUPA-03), OTP fully removed (AUTH-08) |

---

## 1. `src/features/landing/index.tsx` — Hero Stats / Carousel / HERO_ROTATE_MS

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **HERO-01** | `src/features/landing/index.tsx:56-61` | **Important** | **Stats strip is pure placeholder `0` data.** `const heroStats = [{value:'0', label:'Appointments booked'}, {value:'0 min', label:'Average booking time'}, {value:'0', label:'Active doctors'}, {value:'0%', label:'Queue transparency'}]` Rendered at `:153-166` with large `text-3xl` typography. Never fetched — no `useQuery`, no `useAnalytics`, no `usePlatformStats`, no skeleton, no `isPending`. Strip reads as data failure / credibility damage — identical to prior `L-01` / `LAND-01`. No `usePlatformStats` exists in codebase (grep returns 0 hits); only `useAnalytics` (`src/data/hooks.ts:624-631`) and `analyticsRepository.getSummary` (`src/data/supabase/repos.ts:706-809`) exist, but they require `clinicId` and are gated behind `ClinicProvider` (authenticated). Public landing is anon, so neither is usable without a public RPC. **Fix:** either (a) wire to a public RPC like `list_public_doctors` count + `select count(*) from appointments` via `supabase.rpc('get_public_stats')` with 5-min SWR cache, or (b) remove the strip until data exists. Do not ship zeroes to production. Suggested: `const {data: stats}=useQuery({queryKey:['public-stats'], queryFn:()=>supabase.rpc('get_public_stats'), staleTime: 5*60*1000})` then `value: stats?.appointments ?? '—'`. |
| **HERO-02** | `src/features/landing/index.tsx:65-79` | **Minor** | **Single-image carousel churn.** `const heroImages = ['/images/hero-waiting-room.jpg']` (length 1), `const HERO_ROTATE_MS = 5000`, `useEffect(() => setTimeout(()=>setActiveIndex((i+1)%heroImages.length), HERO_ROTATE_MS), [activeIndex])`. Modulo 1 is always `0`; timer restarts forever (≈12 cycles/min) for no visual change, waking the thread each 5 s. Dot nav at `:133-147` renders 1 dot (useless, meets WCAG target-size but conveys no navigation). **Fix:** guard timer + dots: `if (heroImages.length <= 1) return;` before `setTimeout`, and `if (heroImages.length <= 1) return null` for dots container. When adding images, keep `HERO_ROTATE_MS=5000` (standard) but add `prefers-reduced-motion` check: `if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;`. |
| **HERO-03** | `src/features/landing/index.tsx:83-96` (`style={{backgroundImage: `url('${image}')`}}`) | **Minor** | **URL quoting fragile + no error handling.** Single-quote wrapping breaks if asset path contains `'`. Use `url("${image}")` + `encodeURI` or `CSS.escape(image)`. Also no `onError` / fallback — 404 gives blank hero with only `bg-slate-950/70` overlay and no visible CTA context. Add `img` pre-load check or fallback gradient. |
| **HERO-04** | `src/features/landing/index.tsx:89-90` | **Info** | `index === activeIndex ? 'opacity-100' : 'opacity-0'` with `duration-[1500ms]` is correct for single image (always opaque). No bug. |
| **HERO-05** | `src/features/landing/index.tsx:102-147` | **Info** | Hero CTA links `to='/book'` and `to='/sign-up'` are correctly typed; `CalendarCheck` icon has no `aria-hidden` but sibling text exists, so AT announces correctly. No bug. |

---

## 2. `src/features/landing/components/pricing-section.tsx` — Yearly vs Monthly, Switch A11y, Free Banner

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **PRICE-01** | `src/features/landing/components/pricing-section.tsx:10-76,172-174,235-246` | **Info** | **Pricing CTAs correctly go to `/create-clinic?plan=` with the right plan value.** Every tier defines `to: '/create-clinic' as const, search: {plan: 'free'\|'starter'\|'professional'\|'enterprise' as const}` (`freePlan:19-21`, `plans:23-76`). CTA renders `<Link to={plan.to} search={plan.search}>` at `:173` (free) and `:245` (cards). `src/features/create-clinic/index.tsx:41-48` `getInitialPlan()` validates `params.get('plan') ∈ {'free','starter','professional','enterprise'}` else fallback `'free'` — survives tampered query. Price copy now renders `/mo billed yearly` suffix (`:231` `billed yearly` when yearly) fixing prior `/yr` confusion. No dead link. |
| **PRICE-02** | `src/features/landing/components/pricing-section.tsx:78-129` | **Important** (a11y) | **Switch a11y: `aria-label` present but no visible `<label htmlFor>` association.** `const id = useId()` at `:80`, `<Switch id={id} aria-label='Toggle billing period' checked={billing==='yearly'} onCheckedChange={...}>` at `:119-126`. Radix Switch is accessible via `aria-label`, so screen readers hear "Toggle billing period, switch". However the visible `Monthly`/`Yearly` `<span>` labels (`:109-114`, `:129-137`) are plain text with color-only active state (`text-foreground` vs `text-muted-foreground` at `:111-114`, `:130-134`) — not associated via `htmlFor`/`id`. Sighted low-vision users rely on color alone (WCAG 1.4.1). Also `Save 20%` badge (`:139-146`) uses `opacity-50` when monthly — still visible dimmed but conveys state only via opacity. **Fix:** wrap toggle in `<label htmlFor={id}>` or add `aria-labelledby` pointing to the visible spans, and add `aria-pressed` or `role="group" aria-label` context. Add checkmark or underline to active label besides color. |
| **PRICE-03** | `src/features/landing/components/pricing-section.tsx:151-175` | **Info** | **Free banner is intentional horizontal banner, not a 4th card — correct.** Renders `flex-col sm:flex-row` dashed border at `:155` with `freePlan.features` checklist and `Start free` CTA. Not a pricing-card grid member, avoids 4-col layout shift on desktop. `₦0` bubble hidden on mobile (`hidden sm:flex` at `:158`) — minor visual regression on mobile (no price cue) but acceptable as banner text includes "Free". No bug. |
| **PRICE-04** | `src/features/landing/components/pricing-section.tsx:204-233` | **Info** | **Yearly vs monthly display correct.** `value={billing==='monthly' ? plan.monthly : plan.yearly}` via `NumberFlow` at `:219`. `monthly: 15000 → yearly: 12000` (20% discount) matches `Save 20%` badge. Suffix ` /mo{billing==='yearly' && 'billed yearly'}` at `:231` clarifies semantics. No bug. |
| **PRICE-05** | `src/features/landing/components/pricing-section.tsx:1` | **Info** | `'use client'` is Next.js directive; no effect in Vite but harmless. No bug. |

---

## 3. `src/routes/(auth)/sign-in.tsx` + `sign-up.tsx` + `src/features/auth/*` — Auth Flows, OTP Removal, Password Reset

### 3.1 Route shells

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-01** | `src/routes/(auth)/sign-in.tsx:1-12` | **Info** | Correct: `searchSchema = z.object({redirect: z.string().optional()})` and `validateSearch`. No transform, so `redirect` stays raw string for `useSearch`. See AUTH-06 for validation gap. |
| **AUTH-05** | `src/features/auth/sign-up/index.tsx:5-6` | **Important** | **Top-level `window.location.search` outside component.** `const search = new URLSearchParams(window.location.search); const isBusiness = search.get('business') === 'true'` executes at **module import time**, not inside `SignUp()`. In SSR / Vitest `jsdom` without `window`, this throws `ReferenceError: window is not defined`. Even in SPA, value is frozen at first import and never updates on client navigation (e.g., user navigates `/sign-up?business=true` via TanStack `navigate` — `isBusiness` stays `false`). **Fix:** move inside component: `const isBusiness = useMemo(()=> new URLSearchParams(window.location.search).get('business')==='true', [window.location.search])` or read via `useSearch({from:'/(auth)/sign-up'})` after adding `validateSearch` (currently missing on sign-up route, unlike sign-in). Also `src/routes/(auth)/sign-up.tsx:1-6` has **no `validateSearch`** — `?plan=` or `?business=` are parsed ad-hoc, bypassing router validation. |
| **AUTH-09** | `src/features/auth/auth-layout.tsx:1-123` | **Info** | Layout correctly renders `back` link via `Link to={back.to}` with `AuthBackTo` union (`'/' | '/sign-in' | '/sign-up' | '/forgot-password'`) — type-safe, no open redirect in layout itself. No bug. |

### 3.2 Sign-in — `src/features/auth/sign-in/components/user-auth-form.tsx:1-176`

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-02** | `src/features/auth/sign-in/components/user-auth-form.tsx:54-119` (full `onSubmit`) | **Important** | **Missing `too many requests` / rate-limit branch.** Handles `Invalid login credentials` (`:67-70`) → "Wrong email or password..." and `Email not confirmed` (`:71-74`) → "Please confirm your email...". Else fallback `toast.error(msg || 'Could not sign you in')` at `:75-76`. Supabase Auth rate-limited response is `429` with `msg.includes('Too many requests')` / `msg.includes('rate limit')` / `msg.includes('Email rate limit exceeded')` / `msg.includes('For security purposes')`. Those fall into generic `Could not sign you in. Please try again` with no `Retry-After` guidance. **Fix:** add branch before generic: `else if (msg.toLowerCase().includes('too many') || msg.toLowerCase().includes('rate limit') || msg.toLowerCase().includes('for security')) { toast.error('Too many attempts. Please wait a minute and try again.') }`. Also check for `authError.status === 429` when available. Add test mirroring `user-auth-form.test.tsx:189-206` (currently covers invalid credentials only — add 429 and email-not-confirmed cases). |
| **AUTH-03** | `src/features/auth/sign-in/components/user-auth-form.tsx:71-74` + `supabase/config.toml:158-159` | **Important** | **`Email not confirmed` branch is dead code today.** `user-auth-form.tsx:71` checks `msg.includes('Email not confirmed')`, but `supabase/config.toml:158` sets `enable_confirmations = false` (`[auth.email] enable_confirmations = false`). When confirmations disabled, Supabase never returns `Email not confirmed` — every sign-up is auto-confirmed. Branch is harmless but misleading for future prod toggle. Either set `enable_confirmations = true` for prod (and keep the branch) or add comment `// only when enable_confirmations=true` and ensure `supabase/config.toml:159` comment documents prod override. Cross-check `_supabase/auth.getSession` flow: `use-supabase-auth-sync.ts:13` handles `SIGNED_IN` without checking `email_confirmed_at`. |
| **AUTH-06** | `src/features/auth/sign-in/components/user-auth-form.tsx:108-113` + `src/routes/_authenticated/route.tsx:11-18` + `src/routes/(auth)/sign-in.tsx:5-7` | **Critical** | **Open redirect via unvalidated `?redirect=` → `navigate({to: targetPath})`.** `sign-in.tsx:5` `redirect: z.string().optional()` accepts any string including `https://evil.com`, `//evil.com`, `javascript:alert(1)`. `user-auth-form.tsx:111` `const targetPath = redirectTo || defaultPath; navigate({to: targetPath, replace:true})` passes it directly to TanStack Router's `navigate`. While TanStack validates `to` against route tree in strict mode, `to` as arbitrary string can still navigate to external origin if router falls back to `window.location` or if `redirect` contains `http://127.0.0.1:3000` + path. `src/routes/_authenticated/route.tsx:17` throws `redirect({to:'/sign-in', search:{redirect: location.href}})` where `location.href` is the full href (e.g., `/admin/dashboard?foo=bar`) — consistent, but attacker can craft `https://app.example.com/sign-in?redirect=https://evil.com` and after auth be sent off-site, stealing the freshly minted session via Referer or phishing. **Fix:** validate `redirect` starts with `/` and does not contain `://` or `//` after first char: `const safe = redirectTo && redirectTo.startsWith('/') && !redirectTo.startsWith('//') && !redirectTo.includes('://') ? redirectTo : null` or use `z.string().refine(s=>s.startsWith('/') && !s.includes('://'))` in `searchSchema`. Also normalize `location.href` to `location.pathname + location.searchStr` instead of full href if TanStack's `location.href` ever includes origin. |
| **AUTH-10** | `src/features/auth/sign-in/components/user-auth-form.tsx:81-91` | **Minor** | **Profile fetch failure shows generic toast but leaves spinner.** `if (profileError || !profile) { setIsLoading(false); toast.error('Could not load your profile...'); return }` — correctly resets `isLoading`, but `auth.setUser` not called, so `useSupabaseAuthSync` will still fire `SIGNED_IN` and re-set user via `profiles` fetch (race). Not a leak, but double toast possible. Acceptable. |
| **AUTH-11** | `src/features/auth/sign-in/components/user-auth-form.tsx:93-106` | **Info** | Role mapping `const role = [String(profile.role)]` is intentional single-role array (matches `hasRole` checks). No bug. |
| **AUTH-12** | `src/features/auth/sign-in/components/user-auth-form.tsx:151-156` | **Info** | `Forgot password?` link rendered absolute positioned inside password `FormItem` — visually overlaps `FormMessage` when error shows, but not a functional bug. Consider moving outside `FormItem`. |

### 3.3 Sign-up — `src/features/auth/sign-up/components/sign-up-form.tsx:1-168`

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-04** | `src/features/auth/sign-up/components/sign-up-form.tsx:69-83` + `src/data/supabase/repos.ts:615-660` | **Important** | **Duplicate email handling shows raw Supabase message, not friendly toast.** `sign-up-form.tsx:79` `return error instanceof Error ? error.message : 'Error creating account.'` surfaces `AuthApiError: User already registered` verbatim. `src/features/create-clinic/index.tsx:324-328` maps it correctly (`msg.includes('already') ? 'An account with this email already exists. Please sign in instead.'`). Sign-up form should do same. `authRepository.signUp` (`src/data/supabase/repos.ts:615-626`) throws `error` raw from `supabase.auth.signUp` without mapping; test `sign-up-form.test.tsx:106-121` asserts success path but has no duplicate test. **Fix:** in `onError` branch, `if (msg.toLowerCase().includes('already')) return 'An account with this email already exists. Please sign in instead.'`. |
| **AUTH-13** | `src/features/auth/sign-up/components/sign-up-form.tsx:59-82` | **Minor** | **`isLoading` reset relies on `toast.promise` callbacks, not `finally`.** `setIsLoading(true)` at `:60`, reset at `:72` (success) and `:77` (error) inside `toast.promise` handlers. If promise never settles (network hang), button stays disabled forever with no timeout. `src/data/supabase/repos.ts:643` patient insert timeout not bounded. Add `promise.finally(()=>setIsLoading(false))` or use `useMutation` pattern like sign-in. |
| **AUTH-08** | `src/features/auth/*` (grep `OTP`, `otp`, `input-otp`) | **Info** | **OTP removal verified — no OTP flow remains.** Only references are `src/components/ui/input-otp.tsx` (unused, no import in auth), and `supabase/config.toml:126-127` `otp_length=6 / otp_expiry=3600` (config for email OTP if enabled, not active). No `signInWithOtp`, `verifyOtp`, `InputOTP` usage in `src/features/auth/*`. Removal complete. No bug. |
| **AUTH-14** | `src/features/auth/sign-up/components/sign-up-form.tsx:22-39` | **Info** | `phone` validation `z.string().min(7)` allows non-numeric — same as booking (B-01) but out of scope for this hunt. Not flagged as auth-blocking. |

### 3.4 Forgot / Change Password — `forgot-password-form.tsx:1-82`, `forgot-password/index.tsx:1-26`, `change-password/index.tsx:1-196`

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-07** | `src/features/auth/forgot-password/components/forgot-password-form.tsx:40-42` | **Info** | **`redirectTo` correctly uses `window.location.origin` (dynamic, not hardcoded).** `redirectTo: `${window.location.origin}/change-password``. No hardcoded `127.0.0.1` / `getmediq.vercel.app`. Good — survives env promotion. Success toast is enumerate-safe: `Reset link sent to ${email} if the account exists.` at `:52`. Correct to not leak existence. |
| **AUTH-15** | `src/features/change-password/index.tsx:51-89` | **Minor** | **Recovery hash parsing is fragile for hash with leading `?`.** `new URLSearchParams(hash.replace('#',''))` where `hash` is `#access_token=...&refresh_token=...&type=recovery`. Works. However if Supabase ever sends `#access_token=...#` trailing or `?` encoded, `replace('#','')` only strips first `#`. Use `hash.substring(1)` or `new URLSearchParams(hash.slice(1))`. Also no `try/catch` around `supabase.auth.setSession` error — it does `then(({error})=>{if(error) toast...})` without `catch` for network throw. |
| **AUTH-16** | `src/features/change-password/index.tsx:85-89` | **Minor** | **Invalid session redirects with `replace:true` but toast may not show before nav.** `toast.error('No valid reset session...'); navigate({to:'/forgot-password', replace:true})` fires in effect; Toaster is mounted at `__root.tsx:31`, so toast will appear on next page, okay. No bug. |

### 3.5 Auth redirect preservation — does `?redirect=` survive?

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-06** (see above) | `src/routes/_authenticated/route.tsx:11-18` → `src/routes/(auth)/sign-in.tsx:6` → `src/features/auth/sign-in/index.tsx:6` → `user-auth-form.tsx:111` | **Critical** | **Chain is: `_authenticated` `beforeLoad` throws `redirect({to:'/sign-in', search:{redirect: location.href}})` → sign-in shell reads `useSearch` → passes `redirectTo` prop → `navigate({to: targetPath})`.** This correctly preserves the original destination (e.g., `/admin/dashboard`, `/patient`). However `location.href` may include origin + search + hash; after fix to validate `startsWith('/')`, the chain works. Additional gap: `src/routes/patient.tsx:8-14` re-implements the same `redirect` guard without importing shared helper — duplicate logic, easy to drift. Also `src/routes/(auth)/sign-up.tsx` has no `validateSearch`, so `?redirect=` is lost if user goes sign-up → sign-in via "Already have an account? Sign in" link (`auth-layout` `Link to='/sign-in'` without forwarding `search`). That link drops `redirect`. **Fix:** make sign-up link preserve redirect: `<Link to='/sign-in' search={{redirect}}>` when present. |

---

## 4. `supabase/config.toml` — `site_url`, `redirect_urls`, JWT Expiry

_File: `C:/Users/MAFUYAI/Documents/MediQ/supabase/config.toml` (project-level, not `mediq-admin/supabase/` which does not have a config). Project ID `snvdwamqjreuhtyrrrlg`._

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **SUPA-01** | `supabase/config.toml:158` | **Minor** | **Hardcoded `site_url` for local dev — not an env var.** `site_url = "http://127.0.0.1:3000"` (`[auth] site_url`). This is **local-dev default** and is correct for `supabase start` (matches `vite.config.ts:25` `host:'127.0.0.1', port:3000`). For production, `site_url` is overridden via Dashboard `Auth → URL Configuration` or `supabase link` env, not via `config.toml` commit. No `getmediq.vercel.app` hardcoded — grep across repo returns 0 hits in `src/`. Verdict: **no change required**, but document that prod deploy must set Dashboard `Site URL = https://getmediq.vercel.app` and not rely on config file. Prior report `URL-01` noted same. |
| **SUPA-02** | `supabase/config.toml:163` | **Important** | **`additional_redirect_urls` uses wrong scheme `https://` for loopback.** `additional_redirect_urls = ["https://127.0.0.1:3000"]`. Loopback `127.0.0.1` never serves TLS locally (no cert), so `https://127.0.0.1:3000` never matches the actual `http://127.0.0.1:3000` auth redirect (`forgot-password-form.tsx:41` + `staff-dialog.tsx:107` both use `window.location.origin` which is `http://` locally). Supabase Auth will reject the redirect with `redirect URL not allowed`. **Fix:** change to `["http://127.0.0.1:3000"]` (or add both `http` and `https` entries). Also add prod URL explicitly if config is ever used beyond local: `["http://127.0.0.1:3000", "https://getmediq.vercel.app", "https://getmediq.vercel.app/change-password"]`. |
| **SUPA-03** | `supabase/config.toml:164` | **Info** | **JWT expiry 3600 s (1 h) — standard, correct.** `jwt_expiry = 3600`. Docs state default `3600`, max `604800`. `src/lib/supabase.ts:22` `autoRefreshToken: true` + `persistSession: true` handles rotation. `use-supabase-auth-sync.ts:38` sets `exp: session.expires_at*1000` or `Infinity` fallback. No bug. |
| **SUPA-04** | `supabase/config.toml:154-171` (`[auth.email]`) | **Important** | **`enable_confirmations = false` + `enable_signup = true` + `double_confirm_changes = true`.** With `enable_confirmations=false`, sign-ups are auto-confirmed, so `sign-in` branch `Email not confirmed` (AUTH-03) never fires and `authRepository.signUp` never requires confirmation email. This is correct for **local dev** but **must be `true` in prod** if you want to enforce email ownership before clinic creation. If left `false` in prod, any attacker can sign up with arbitrary `email` and create a clinic impersonating that email. Document prod requirement: `enable_confirmations=true` + SMTP configured (`[auth.email.smtp]` is commented out, so local uses `local_smtp:54324`). |
| **SUPA-05** | `supabase/config.toml:101-103,137-143` | **Minor** | **No explicit `additional_redirect_urls` for prod — implied env override.** No `getmediq.vercel.app` entry. If team ever runs `supabase db push --include-all` expecting config to carry prod URL, it will overwrite dashboard setting with loopback. Add comment: `# Production: set Auth → URL Configuration in Dashboard to https://getmediq.vercel.app; do not deploy this file's site_url to prod`. |
| **SUPA-06** | `supabase/config.toml:170-192` (`[auth.rate_limit]`) | **Info** | Rate limits are present: `email_sent=2`, `sms_sent=30`, `sign_in_sign_ups=30` per 5 min per IP, `token_verifications=30`. These back the 429 case that `user-auth-form.tsx` does not handle (AUTH-02). Config is correct. |

---

## 5. `src/lib/supabase.ts` — Client Creation, Storage, Realtime

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **SUPA-07** | `src/lib/supabase.ts:1-32` | **Minor** | **Client creation is correct but missing explicit `flowType` and storage key.** `createClient(supabaseUrl, supabaseAnonKey, {auth:{persistSession:true, autoRefreshToken:true, detectSessionInUrl:true}})`. `persistSession` uses `localStorage` under key `sb-<project-ref>-auth-token` by default — fine. However no `flowType: 'pkce'` explicit (supabase-js v2 defaults to `pkce` since 2024, so okay, but pin it to avoid implicit downgrade if default changes). Also no `storageKey` customization — if app ever shares origin with another Supabase project (e.g., preview deployments), keys could collide. Add `storageKey: 'mediq-auth'` if multi-project origin. `detectSessionInUrl:true` is required for `change-password` hash flow (`change-password/index.tsx:63` `setSession`) — correct. No `realtime` config here; realtime is enabled per-table via `supabase/config.toml:81` `realtime.enabled=true` and per-hook `useRealtimeTable` (`src/data/hooks.ts:638-680`). No bug, but add comment. |
| **SUPA-08** | `src/lib/supabase.ts:3-11` | **Info** | **Env guard correctly throws if `VITE_SUPABASE_URL` or `VITE_SUPABASE_ANON_KEY` missing, with guidance to copy `.env.example`.** Good. `.env.example:1-9` correctly warns `DO NOT add VITE_SUPABASE_SERVICE_ROLE_KEY` and points to Edge Function secrets. No hardcoded URL — `grep 127.0.0.1` returns 0 hits in `src/`, 0 `getmediq` hits in `src/`. No bug. |
| **SUPA-09** | `src/lib/supabase.ts:26-32` | **Info** | **Service role comment is correct.** States `supabaseAdmin was removed` and privileged calls go via `invite-staff` Edge Function reading `SUPABASE_SERVICE_ROLE_KEY` from Secrets — matches `supabase/functions/invite-staff/index.ts:27-31` which reads `Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")`. Good. |

---

## 6. Hardcoded `http://127.0.0.1` / `getmediq.vercel.app` URLs — Should They Be Env Vars?

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **URL-01** | `vite.config.ts:25` | **Info** | `host:'127.0.0.1', port:3000` — dev server bind, not a runtime URL. Correct to keep hardcoded for local dev. No env var needed. |
| **URL-02** | `src/` (grep `127\.0\.0\.1\|getmediq\.vercel\.app` → 0 hits) | **Info** | **No hardcoded `127.0.0.1` / `getmediq.vercel.app` in `src/`.** All URLs are `window.location.origin` (`forgot-password-form.tsx:41`, `check-in/index.tsx:83`, `booking/index.tsx:79`, `create-clinic/index.tsx:42` via `window.location.search`, `qr-ticket.tsx:20`, `staff-dialog.tsx:107`) or `import.meta.env.VITE_SUPABASE_URL`. Good — 12-factor. |
| **URL-03** | `supabase/config.toml:99,158,163` | **Info/Minor** | `api_url="http://127.0.0.1"` (Studio), `site_url="http://127.0.0.1:3000"`, `additional_redirect_urls=["https://127.0.0.1:3000"]` — local defaults. Not env-vars by design (Supabase CLI config is local-only). Prod URL lives in Dashboard, not here. Only fix needed is `SUPA-02` scheme mismatch. |
| **URL-04** | `supabase/functions/invite-staff/index.ts:6` | **Info** | `ALLOWED_ORIGINS` fallback is `"https://getmediq.vercel.app,http://localhost:3000"` — correctly env-driven via `Deno.env.get("ALLOWED_ORIGINS")` with prod default, not hardcoded origin in CORS header. Prior wildcard `*` has been fixed (was `INV-07`). Good. |

---

## 7. Cross-Cutting / Auth Flow Integrity

| # | File:Line | Severity | Finding |
|---|-----------|----------|---------|
| **AUTH-16** | `src/features/create-clinic/index.tsx:307-359` (CombinedForm) `+` `src/data/supabase/repos.ts:643-654` | **Minor** | **On sign-up, `patients` insert omits `clinic_id`.** `authRepository.signUp` does `supabase.from('patients').insert({name, phone, email, visits:0})` without `clinic_id`. If RLS requires `clinic_id` (tenancy), row lands as `clinic_id IS NULL` and is invisible to clinic staff (mirrors old `INV-01`). Should either omit the insert (defer to first booking) or pass `clinic_id` from `getInitialPlan` context. Low impact because `patients` RLS currently has anon-insert path, but drift risk. |
| **AUTH-17** | `src/hooks/use-supabase-auth-sync.ts:1-53` | **Info** | Correctly preserves `clinicId/clinicRole/clinicName` only when `prev.accountNo === session.user.id` — prevents clinic leak across browser-account switch. Good. |
| **AUTH-18** | `src/routes/change-password.tsx:1-6` `+` `src/features/change-password/index.tsx:51-52` | **Info** | No `validateSearch` / `beforeLoad` guard — anyone can visit `/change-password` without token; component shows `invalid` → redirect to `/forgot-password`. Correct. |

---

## 8. Fix Priority (no code changed in this pass)

1. **Immediate (Critical):** Validate `?redirect=` (AUTH-06) — enforce `startsWith('/')` and no `://` in `sign-in.tsx:searchSchema` + `user-auth-form.tsx:111`, and preserve `redirect` through sign-up's "Sign in" link.
2. **Next (Important):** Add `429 / rate limit` branch to sign-in (AUTH-02); fix `additional_redirect_urls` scheme to `http://127.0.0.1:3000` (SUPA-02); decide `enable_confirmations` prod value and document (SUPA-04); move sign-up `window.location.search` inside component + add `validateSearch` (AUTH-05); map duplicate email to friendly toast (AUTH-04).
3. **Polish:** Wire hero stats to public RPC or remove strip (HERO-01); disable carousel timer/dots when `length<=1` (HERO-02); fix `url('...')` quoting (HERO-03); add visible label association for pricing Switch (PRICE-02); pin `flowType:'pkce'` in supabase client (SUPA-07).

---

*Report written to `docs/reports/bug-bounty-final-hero-auth.md` (`mediq-admin/docs/reports/bug-bounty-final-hero-auth.md`). Citations are `file:line` for direct navigation. No code modified.*
